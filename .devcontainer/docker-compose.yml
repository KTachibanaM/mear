version: '3.8'
services:
  devcontainer:
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../..:/workspaces:cached
    command: sleep infinity
    networks:
      - mear-network
  minio-source:
    image: minio/minio:RELEASE.2023-02-27T18-10-45Z.hotfix.0fb43447e
    restart: unless-stopped
    command:
      - server
      - /data
    volumes:
      - minio-source-data:/data
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    networks:
      - mear-network
    healthcheck:
      test:
        - CMD
        - curl
        - -f
        - http://localhost:9000/minio/health/live
      interval: 30s
      timeout: 20s
      retries: 3
  minio-destination:
    image: minio/minio:RELEASE.2023-02-27T18-10-45Z.hotfix.0fb43447e
    restart: unless-stopped
    command:
      - server
      - /data
    volumes:
      - minio-destination-data:/data
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    networks:
      - mear-network
    healthcheck:
      test:
        - CMD
        - curl
        - -f
        - http://localhost:9000/minio/health/live
      interval: 30s
      timeout: 20s
      retries: 3
  minio-agent-binary:
    image: minio/minio:RELEASE.2023-02-27T18-10-45Z.hotfix.0fb43447e
    restart: unless-stopped
    command:
      - server
      - /data
    volumes:
      - minio-agent-binary-data:/data
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    networks:
      - mear-network
    healthcheck:
      test:
        - CMD
        - curl
        - -f
        - http://localhost:9000/minio/health/live
      interval: 30s
      timeout: 20s
      retries: 3
volumes:
  minio-source-data:
  minio-destination-data:
  minio-agent-binary-data:
networks:
  mear-network:
    external: true
